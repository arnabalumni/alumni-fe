export type ChildrenProp = {
  children: React.ReactNode;
};

export interface Alumni {
  [key: string]: any;
  id: number;
  name: string;
  occupation: string;
  address: string;
  email: string;
  linkedin: string;
}
